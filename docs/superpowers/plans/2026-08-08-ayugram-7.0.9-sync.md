# AyuGram 7.0.9 Release & Fixes Integration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Integrate all updates and bug fixes from official AyuGram Desktop release 7.0.9 (`origin/dev`) into local `dev` branch while preserving local custom improvements.

**Architecture:** Perform a git merge of `origin/dev` into local branch `dev`. Update submodules, resolve conflicts (if any) favoring local custom UI fixes while keeping upstream core features, and verify git log and status.

**Tech Stack:** Git, C++, CMake.

## Global Constraints
- Do not overwrite local custom UI improvements (smooth music button slide bounds, profile music background immediate initialization, macOS ca-certificates CI fix).
- Incorporate upstream 7.0.9 features (rich formatted repeat and AyuForward messages, streamer mode rework, translator batch leak fix, D-Bus cmake workaround, miniapp crash fixes).

---

### Task 1: Merge upstream origin/dev into dev

**Files:**
- Modify: `c:\Users\aver\Desktop\ayu\AyuGramDesktop-dev` git working tree

- [ ] **Step 1: Execute git merge origin/dev**

Run: `git merge origin/dev --no-commit`
Expected: Merge initiated or completed cleanly without auto-commit.

- [ ] **Step 2: Check merge status and resolve conflicts if any**

Run: `git status`
Expected: Review modified/conflicting files. If any conflicts, resolve them retaining local UI fixes and upstream core improvements.

- [ ] **Step 3: Update and sync submodules**

Run: `git submodule update --init --recursive`
Expected: Submodules synchronized to requested commits.

- [ ] **Step 4: Complete merge commit**

Run: `git commit -m "feat: merge official AyuGram Desktop release 7.0.9 updates and fixes"`
Expected: Clean merge commit created on branch `dev`.

- [ ] **Step 5: Verify git log and status**

Run: `git log -n 10 --oneline`
Expected: `dev` branch includes both upstream 7.0.9 commits and local commits cleanly.
